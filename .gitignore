node_modules
.DS_Store
.env
.env.local
.env.production.local
.env.vercel*
.env*.bak.claude
uploads/
.next

# Vercel (lien local, ne pas versionner)
.vercel

# TypeScript — cache incrémental
*.tsbuildinfo

# Cursor — journaux de debug uniquement (settings.json peut rester partagé)
.cursor/debug*.log

# Claude / agents locaux
.claude/

# Copies flottantes a conserver hors repo
.agents/Delivery Syst/
SAFE Inc./

# Brouillons locaux (HTML de travail, PDF generes a la main). Rien de reproductible.
tmp/

# Relevés et captures de référence de l'application (docs/design/PROCEDURE_EXTRAITS_VITRINE.md).
# Ils sont RÉGÉNÉRABLES par scripts/capturer-ecran-reel.mjs et
# scripts/relever-depuis-la-base.mjs, et ils portent des noms de clients et des
# montants du cabinet de démonstration. Une preuve se refait, elle ne se
# versionne pas.
docs/design/references-app/
