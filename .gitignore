node_modules
.DS_Store
.env
.env.local
.env.production.local
.env.vercel*
.env*.bak.claude
uploads/
.next

# Vercel (lien local, ne pas versionner)
.vercel

# TypeScript — cache incrémental
*.tsbuildinfo

# Cursor — journaux de debug uniquement (settings.json peut rester partagé)
.cursor/debug*.log

# Claude / agents locaux
.claude/

# Copies flottantes a conserver hors repo
.agents/Delivery Syst/
SAFE Inc./
