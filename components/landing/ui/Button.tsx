import { Button as BaseButton } from "@/components/ui/Button";

// On landing pages, we simply export the universal App Button
// as it precisely controls all marketing and app variants via the SAFE DS v1.0
export const Button = BaseButton;
