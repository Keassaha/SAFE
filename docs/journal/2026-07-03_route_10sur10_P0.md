# 2026-07-03 — Route vers 10/10 : P0 « protéger l'existant » cleared

Objectif CEO : faire passer la maturité produit de 7/10 à 10/10. Approche : incréments
vérifiés, bloqueurs dans l'ordre. Ce lot ferme le tier P0.

## Livré et vérifié
1. **Pages blanches /temps et /comptes (P0)** — CORRIGÉ + vérifié à l'écran.
   `components/layout/PageTransition.tsx` rendait opacity:0 au SSR (framer-motion) puis
   animait à 1 côté client ; sous une frontière Suspense qui suspend, l'animation JS ne se
   déclenchait pas → page invisible. Remplacé par l'animation CSS `animate-fade-in`
   (safe-fade-in), qui se termine toujours (repos = opacity:1). Mode d'échec éliminé.
   Vérifié : /comptes et /temps affichent leur contenu (captures).
2. **Équivalence facture écran = PDF = portail (P0)** — VERROUILLÉ.
   Test de contrat `invoice-presenter-equivalence.test.ts` (7 tests) : `presentInvoice` est
   un projecteur fidèle et déterministe ; les 3 rendus consomment cette source unique
   (InvoiceDocument sert aperçu ET PDF ; portail public via presentInvoice). Risque
   « 15 000 $ écran / 12 500 $ PDF » fermé.
3. **Rate-limit PDF facture (P0)** — ajouté (30/min/cabinet) sur
   `GET /api/facturation/factures/[id]/pdf` (génération @react-pdf coûteuse).

## Déjà OK (audit map, rien à faire)
- Rate-limit PDF audit gratuit : déjà présent.
- Gate console : `isSafeInternalUser` utilise déjà `User.isInternal` (repli transitoire sur
  nom cabinet). Aucun secret en clair détecté.

## État tests
715 tests verts (82 fichiers), tsc propre. Prod inchangée pour la conformité (flag éteint).

## Reste vers 10/10 (P1)
Onboarding qui persiste, taxonomie configurée branchée au formulaire dossier, fermeture de
dossier + rétention (déjà bien avancé : blocages + calcul rétention + audit ; reste soft-lock
des listes + job de purge), bouton payer sur facture publique, création rapide client.
Puis P2 (monitoring, matching bancaire, rapports PDF).
