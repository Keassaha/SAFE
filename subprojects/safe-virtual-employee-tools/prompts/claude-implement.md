# Claude Code Daily Implementation Prompt Template

You are implementing one daily batch for the SAFE subproject:
`subprojects/safe-virtual-employee-tools`.

Before coding, read:

- root `CLAUDE.md`
- `subprojects/safe-virtual-employee-tools/CLAUDE.md`
- the relevant spec in `subprojects/safe-virtual-employee-tools/specs/`
- today's daily plan in `subprojects/safe-virtual-employee-tools/daily/`

Implement only the batch described below.

Batch:

```text
<paste daily batch here>
```

Rules:

- Keep changes narrowly scoped.
- Preserve SAFE multi-tenant safety.
- Do not implement future tools.
- Do not add AI behavior unless the daily plan explicitly asks for it.
- Validate with tests or documented manual checks.

End with:

1. What was completed
2. Files changed
3. What remains
4. Exact next prompt for the next batch
